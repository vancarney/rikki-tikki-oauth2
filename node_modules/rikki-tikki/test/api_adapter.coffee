{_}             = require 'underscore'
express         = require 'express'
(chai           = require 'chai').should()
expect          = chai.expect
API   = require '../src'
# @app = express()
# describe 'API Adapter Test Suite', ->
  # it 'should create an Adapter', =>
    # API.createAdapter 'express', @app
    # API.listAdapters.should.equal 1
